# Copy to frontend/.env if needed. NEVER commit the real .env.
# No frontend env vars are required by default: API calls use relative /api paths
# through the Vite proxy, so no backend URL is baked into the bundle.
# Add VITE_-prefixed vars here only if the app needs build-time config.
